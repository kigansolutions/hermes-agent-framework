# SCHEDULER

Recurring workflow execution — run workflows on a schedule, detect
drift over time.

## Phase: meta (lifecycle)

## Commands

- /schedule create <workflow> --interval [hourly|daily|weekly]
- /schedule list
- /schedule status <id>
- /schedule delete <id>

## Output
- Notifications on new findings
- Drift detection over time
- Aggregated change logs
